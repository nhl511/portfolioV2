interface LinkType {
  title: string;
  path: string;
}

interface itemType {
  title: string;
  imageUrl: string;
}

interface projectType {
  title: string;
  des: string;
  techs: string;
  imgUrl: string;
  url?: string;
  source?: string;
}
